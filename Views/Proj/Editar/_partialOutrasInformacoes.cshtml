@*@model Itau.PO.UI.MVC.ViewModel.ProjetoViewModel

@using (Ajax.BeginForm("SalvarOutrasObservacoes", "Proj", new AjaxOptions { HttpMethod = "POST", OnBegin = "return ValidarCamposOutrasInformacoes()", OnSuccess = "OnSuccess", OnFailure = "OnFailure" }))
{
    @Html.HiddenFor(model => model.IdProjeto)

    <div class="card">
        <div class="card-body">
            <div class="row">
                <div class="col-12">
                    <div class="form-group">
                        <h4>Comentários dos Riscos</h4>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-md-12">
                    <div class="form-group">
                        @Html.LabelFor(model => model.OutrasInformacoes, htmlAttributes: new { @class = "control-label" })
                        @Html.TextAreaFor(model => model.OutrasInformacoes, htmlAttributes: new { @class = "form-control", @rows = "5" })
                        <span id="erroSpanOutrasInformacoes" class="erro field-validation-valid text-danger small">O campo Outras Informações é obrigatório.</span>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-12">
                    <div class="button-group float-right">
                        <input type="submit" class="btn btn-success" value="Salvar" />
                    </div>
                </div>
            </div>
        </div>
    </div>
}*@

@model Itau.PO.UI.MVC.ViewModel.ProjetoViewModel

@using (Ajax.BeginForm("SalvarOutrasObservacoes", "Proj", new AjaxOptions { HttpMethod = "POST", OnBegin = "return ValidarCamposOutrasInformacoes()", OnSuccess = "OnSuccess", OnFailure = "OnFailure" }))
{
    @Html.HiddenFor(model => model.IdProjeto)

    <div class="card">
        <div class="card-body">
            <div class="row">
                <div class="col-12">
                    <h4><label class="control-label">Outras Informações</label></h4>
                </div>
            </div>

            <div class="row">
                <div class="col-md-12">
                    <div class="form-group">
                        @Html.TextAreaFor(model => model.OutrasInformacoes, htmlAttributes: new { @class = "form-control", @rows = "5" })
                        <span id="erroSpanOutrasInformacoes" class="erro field-validation-valid text-danger small">O campo Outras Informações é obrigatório.</span>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-12">
                    <div class="button-group float-right">
                        <input type="submit" class="btn btn-success" value="Salvar" />
                    </div>
                </div>
            </div>
        </div>
    </div>
}